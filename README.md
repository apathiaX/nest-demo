# 习惯养成与挑战规划平台 - 后端服务

## 开发

```bash
pnpm run start          # 启动应用
pnpm run start:dev      # 启动开发模式（热重载）
pnpm run start:debug    # 启动调试模式
```

## 构建和生产

```bash
pnpm run build          # 构建生产版本
pnpm run start:prod     # 启动生产版本
```

## 数据库

```bash
pnpm run prisma:generate  # 生成 Prisma Client
pnpm run prisma:migrate   # 运行数据库迁移
pnpm run prisma:studio    # 打开 Prisma Studio（可视化工具）
```

## 📚 API 文档

启动服务后，访问交互式 API 文档：

**Swagger UI**: http://localhost:3000/api/docs |
